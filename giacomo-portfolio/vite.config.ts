import { defineConfig } from 'vite'

export default defineConfig({
  // IMPORTANTE: Sostituisci 'NOME-REPO' con il nome della tua repository!
  // Esempio: se la repo è 'giacomo-portfolio', scrivi '/giacomo-portfolio/'
  base: '/giacomo-portfolio/', 
})
